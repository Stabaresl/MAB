# Dependencias
/node_modules

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Entorno — las claves nunca entran al repositorio
.env
.env*.local

# Material de referencia local, no forma parte del producto
design-rules/
.agents/
.claude/skills/
skills-lock.json
assets/

# El portafolio original pesa 110 MB: git no es el sitio para eso.
# Las fotos que usa el catalogo ya estan procesadas en public/catalogo/.
PORTAFOLIO*.pdf

# Material de origen pesado del visor 360. La secuencia ya esta procesada en
# public/360/grifo-sprite.webp (407 KB); el ZIP son 11 MB de PNG en bruto.
image/*.zip
image/*.mp4

# Salidas de herramientas
.playwright-mcp/

# Supabase local
supabase/.branches
supabase/.temp

# Sistema
.DS_Store
Thumbs.db
*.log
